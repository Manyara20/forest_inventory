import Alert from "./Alert"

const NotificationToast = () => {

  return (
   <div className="fixed top-0 left-0 w-[80%] h-20 flex items-center justify-end z-50">
        < Alert />
   </div>
  )
}

export default NotificationToast